# Static CSS assets may remain aggressively cached because their version hash is stable.
/css/*
  Cache-Control: public, max-age=31536000, immutable

# JavaScript is revalidated so direct GitHub/API commits cannot get stuck behind
# an old immutable asset when the query-string hash was not regenerated locally.
/js/*
  Cache-Control: public, max-age=0, must-revalidate

/favicon.svg
  Cache-Control: public, max-age=31536000, immutable

# HTML Files - Do not cache (Revalidate)
# Ensures users always get the latest version references
/*.html
  Cache-Control: public, max-age=0, must-revalidate
